def draw_stars(arr):
	for val in arr:
		if type(val) == int:   
			print '*' * val
		elif type(val) == str:
			print val[0].lower() * len(val)
 
x = [4, "Tom", 1, "Michael", 5, 7, "Jimmy Smith"]
draw_stars(x)
